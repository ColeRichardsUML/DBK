function DBK_CLI {
    <#
    
    .SYNOPSIS
        Provides a CLI through which the user may interact with the script while it is running
        NEED to figure out sending interrupts to the main script, if at all.
        Forgive me, I am new to PS scripting.
    
    #>
}

param(
    [switch]$GUI
)

if ($GUI) {
    
}